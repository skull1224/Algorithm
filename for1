//n이 7이면 1+2+3+4+5+6+7=28로 출력하는 프로그램을 작성하세요.//

#include <stdio.h>

int main(void)
{
	int n, i,sum=0;
	printf("n값을 입력 :");
	scanf_s("%d", &n);
	for (i = 1; i <= n; i++)
	{
		printf("%d", i);
		if (i < n)
		{
			printf("+");
		}
		sum += i;
	}
	printf("= %d", sum);

}
