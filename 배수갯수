#include <stdio.h>

int main() {

  int a,b,c,cnt=0,tmp;
  scanf("%d %d %d", &a,&b,&c);
  if(a>=b)
    {
      tmp=a;
      a=b;
      b=tmp;
    }
  for(int i=a;i<=b;i++)
  {
    if(i%c==0)
    {
      cnt++;
    }
    
    
  }
  printf("%d",cnt);
  
  return 0;
}
